# -----------------------------------------------------------------------------
# Render / 프로덕션 전용. API 서버에 필요한 패키지만 포함.
# 로컬에서 스크립트·평가 실행 시: pip install -r requirements-dev.txt
# -----------------------------------------------------------------------------

# FastAPI
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Pydantic & Validation
pydantic==2.10.6
pydantic-settings==2.10.1
email-validator==2.1.0
pgvector==0.4.2

# Redis & High Performance
redis==5.0.1
orjson==3.11.7

# HTTP Client
httpx==0.26.0
requests==2.32.3

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# CORS & Env
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2
openai==1.61.0

# Error tracking (optional: set SENTRY_DSN to enable)
sentry-sdk[fastapi]>=1.39.0

# RAG 런타임: BM25 + 벡터 검색. (청킹/인덱스 빌드는 로컬 스크립트에서만 사용)
numpy>=1.24.0,<3
rank_bm25>=0.2.2
faiss-cpu

# RAG 로컬 리랭커는 선택. 원격 API 사용 시 불필요.
# sentence-transformers>=2.2.0
